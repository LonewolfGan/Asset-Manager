You are a staff-level full-stack engineer and product designer with a strong sense of craft. Your task is to build "EverydayTools Hub", a production-ready, highly optimized Next.js 14 (App Router) multi-tool SaaS.

You must strictly obey the following rules. Do NOT generate generic templates, marketing fluff, or "AI-coded" aesthetics. Every decision must be deliberate, performant, and defensible.
1. TECH STACK & ARCHITECTURE

    Framework: Next.js 14 (App Router), strict TypeScript.

    Styling: Tailwind CSS v3 only. DO NOT use component libraries like Shadcn, MUI, or Radix. Build bespoke components.

    i18n: Use next-i18next with /[locale] routing.

    State: React hooks only (no Redux). Use useReducer for complex states like batch processing.

    Storage: LocalStorage only. Never persist sensitive user data.

2. DESIGN DIRECTION: "EDITORIAL UTILITY"

    Typography: DM Serif Display for H1/Display. IBM Plex Sans for UI/body. IBM Plex Mono for data/passwords.

    Palette: Warm off-white background (#F7F6F3), white surfaces (#FFFFFF), near-black text (#1A1916), and a single sharp blue accent (#1A6BFF) for primary actions.

    UX Rules: ZERO emojis. No lorem ipsum. Primary actions must be one click away. Never use infinite spinners; use real progress bars.

3. THE TOOL CATALOGUE (CLIENT-SIDE FIRST)

Implement the following tools as separate routes. Heavy operations must happen in the browser to ensure zero server costs and maximum privacy.

A. Document & E-Book Converters
Implement all common conversion possibilities (like iLovePDF).

    Use pdfjs-dist (via Web Workers) for PDF text extraction, and pdf-lib for document generation.

    Use mammoth to parse DOCX into HTML/TXT on the client.

    CRITICAL: For layout-engine dependent conversions (e.g., DOCX to PDF, ODT to PDF), DO NOT fake the result with rasterized canvas images. You must explicitly mark them as // NOT IMPLEMENTED in the service layer and wire them to a server-side stub that returns a clean error message.

B. Image Converters

    Build a Canvas API pipeline to handle PNG, JPEG, WEBP, AVIF, BMP, GIF, TIFF, ICO, and SVG rasterization.

    Include a quality slider, dimensional resizing, and batch support (up to 20 files with strict memory cleanup using URL.revokeObjectURL).

C. AI Background Remover

    Implement @imgly/background-removal and its peer dependency onnxruntime-web.  

    Execute it inside a Web Worker or dynamically imported component (ssr: false) to prevent blocking the main thread.

D. Metadata & AI Watermark Cleaner

    Media/Documents: Use piexifjs to strip EXIF data from images. Use pdf-lib to reset the Document Information Dictionary and XMP metadata streams in PDFs.  

    AI Text Watermark Scrubber: To prevent AI detection, you must aggressively strip invisible characters. Implement a regex pass to remove zero-width spaces, word joiners, and byte order marks using //g.

    AI Stylistic Scrubber: Create an algorithm to detect and replace "Tier 3 boilerplate phrases" (e.g., "In conclusion", "It is important to note") to break up uniform AI sentence structures. Add a disclaimer that this does not guarantee 100% bypass of all cryptographic fingerprints.  

E. Utilities (Instant Calculation)

    Password Generator: Use window.crypto.getRandomValues(). Calculate actual mathematical entropy bits (E=L×log2​(R)) instead of generic "strong/weak" labels.

    Currency Converter: Fetch rates from Open Exchange Rates, cache in localStorage with a 1-hour TTL, and fall back to static JSON.

    Unit & Daily Calculators: Drive unit conversions via a mathematical graph config file. Percentage/Tip calculators must compute instantly onChange without a "Calculate" button.

4. DELIVERABLES

Generate the complete scaffold for this application. Start by providing the package.json, the Tailwind configuration, the global CSS variables, and the core layout shell. Then, implement the specific tool services (metadataService.ts, documentConversionService.ts, etc.) strictly adhering to the client-side constraints outlined above.